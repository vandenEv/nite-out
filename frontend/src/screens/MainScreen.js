import React from "react";
import {
    View,
    Text,
    TextInput,
    TouchableOpacity,
    StyleSheet,
} from "react-native";

const PublicanSignUpScreen = ({ navigation }) => {
    return <Text Style={styles.mainText}>Main Screen</Text>;
};

const styles = StyleSheet.create({
    mainText: {
        fontSize: 10,
    },
});

export default PublicanSignUpScreen;
